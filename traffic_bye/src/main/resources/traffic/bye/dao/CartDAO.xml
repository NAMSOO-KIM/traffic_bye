<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
      PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
   "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="traffic.bye.dao.CartDAO">
	
	<select id="getCartList" parameterType="long" resultType="CartVO">
	<![CDATA[
			select c.member_id, c.item_id, c.quantity, i.category_id, i.name, i.stock ,i.price, i.detail, i.store_id ,im.upload_file_name, im.real_file_name
			from item i
			inner join item_image im on im.item_id = i.id
			inner join cart c on c.item_id = i.id
			where c.member_id = #{member_id}
	]]>
	</select>
	 
	
	
</mapper>
